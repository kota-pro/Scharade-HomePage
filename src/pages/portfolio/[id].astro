---
export const prerender = false;

import Layout from "../../layouts/Mypost_Layout.astro";
import "../../styles/UA+reset.css";
import { getPortfolioDetail } from "../../lib/microcms";

const { id } = Astro.params;

if (!id) {
  return new Response("Not Found", { status: 404 });
}

let portfolio;
try {
  portfolio = await getPortfolioDetail(id as string);
} catch (e) {
  console.error("Error fetching portfolio:", e);
  return new Response("Not Found", { status: 404 });
}

if (!portfolio) {
  return new Response("Not Found", { status: 404 });
}

const rawGrade = portfolio.grade || "";
const gradeNumber = rawGrade.replace(/[^0-9]/g, ""); 
const gradeText = rawGrade.replace(/[0-9]/g, "").trim(); 

const rawBio = portfolio["self-introduction"] || "";
const formattedBio = rawBio.replace(/\n/g, '<br/>');

const pictures = portfolio.pictures || [];
---

<<<<<<< HEAD
<Layout title={`Portfolio | ${portfolio.name}`} from="Portfolio">
  <div class="profile-section">
    <div class="profile-info">
      <div class="section-head">
        <h2 class="profile-name">{portfolio.name}</h2>
        <div class="social-links">
          {portfolio.instagram && (
            <a href={`https://instagram.com/${portfolio.instagram}`} target="_blank" rel="noopener noreferrer" class="social-icon">
              <img src="/icons/Instagram.svg" alt="Instagram">
            </a>
          )}
        </div>
      </div>

      <div class="profile-meta">
        <p class="tags">#Updated At: {new Date(portfolio.updatedAt).toLocaleDateString()}</p>
        <div>
          <p class="tags">#夜景</p>
          <p class="tags">#Snap Shot</p>
          <p class="tags">#Portrait</p>
        </div>

      </div>

      <div class="profile-bio">
        <p set:html={formattedBio} />
      </div>
    </div>

    <div class="grade-circle-wrapper">
        <div class="grade-circle">
            {portfolio.icon ? (
              <img 
                src={portfolio.icon.url} 
                alt={portfolio.name} 
                class="grade-icon-img"
                width={portfolio.icon.width}
                height={portfolio.icon.height}
              />
            ):(
              <img 
                src="/images/placeholder_icon.jpeg" 
                alt="Placeholder Icon" 
                class="grade-icon-img"
              />
            )}
        </div>
        <div class="grade-text-overlay">
          {gradeNumber && <p class="grade-num">{gradeNumber}</p>}
          {/* {gradeText && <p class="grade-txt">{gradeText}</p>} */}
        </div>
    </div>
  </div>

  <div class="divider-line"></div>

  <div class="photos-section">
    <div class="photos-header">
      <span class="click-hint"><p class="tags">Pictures Count: {pictures.length}</p></span>
      <div class="pagination-simple">◁ 1 / {pictures.length} ▷</div>
    </div>

    <div class="photo-grid">
      {pictures.map((pic) => (
        <div class="photo-item">
            <img 
              src={pic.url} 
              alt="Portfolio Image" 
              width={pic.width} 
              height={pic.height}
              loading="lazy"
            />
        </div>
      ))}
    </div>
  </div>
</Layout>

<style>
=======
<Layout title={`Portfolio | ${portfolio.name}`}>
  <Fragment slot="main-slot">
    <div class="portfolio-detail-container">
      <div class="section-title">
        <h1>Portfolio</h1>
        <div class="section-title-line"></div>
      </div>

      <div class="profile-section">
        <div class="profile-info">
          <h2 class="profile-name">{portfolio.name}</h2>
          
          <div class="social-links">
            {portfolio.instagram && (
              <a href={`https://instagram.com/${portfolio.instagram}`} target="_blank" rel="noopener noreferrer" class="social-icon">
                <span style="font-family: sans-serif;">📷</span>
                <span>Instagram</span>
              </a>
            )}
          </div>

          <div class="profile-meta">
            <p class="tags">#Photo #Portrait</p> 
            <p class="camera-info">Camera Info...</p>
          </div>

          <div class="profile-bio">
            <p set:html={formattedBio} />
          </div>

          <p class="contact-text">コンタクトはInstagramのDMへ</p>
        </div>

        <div class="grade-circle-wrapper">
            <div class="grade-circle">
                {portfolio.icon ? (
                  <img 
                    src={portfolio.icon.url} 
                    alt={portfolio.name} 
                    class="grade-icon-img"
                    width={portfolio.icon.width}
                    height={portfolio.icon.height}
                  />
                ) : (
                  <>
                    {gradeNumber && <span class="grade-num">{gradeNumber}</span>}
                    {gradeText && <span class="grade-txt">{gradeText}</span>}
                  </>
                )}
            </div>
        </div>
      </div>

      <div class="divider-line"></div>

      <div class="photos-section">
        <div class="photos-header">
            <span class="click-hint">click!! ⤵</span>
            <div class="pagination-simple">◁ 1 / {pictures.length} ▷</div>
        </div>

        <div class="photo-grid">
          {pictures.map((pic) => (
            <div class="photo-item">
               <img 
                 src={pic.url} 
                 alt="Portfolio Image" 
                 width={pic.width} 
                 height={pic.height}
                 loading="lazy"
               />
            </div>
          ))}
        </div>
      </div>

    </div>
  </Fragment>
</Layout>

<style>
  .portfolio-detail-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 40px 100px;
    font-family: "Zen Old Mincho", serif;
    color: #333;
  }

  .section-title {
    display: flex;
    align-items: center;
    margin-bottom: 60px;
  }
  .section-title h1 {
    font-family: "Didot", serif;
    font-size: 80px;
    font-weight: 400;
    margin-right: 40px;
    letter-spacing: 0.05em;
  }
  .section-title-line {
    flex-grow: 1;
    height: 1px;
    background: #000;
    margin-top: 15px;
  }

>>>>>>> 246114c9b5c422ea4b070944e480aabcd002ad44
  .profile-section {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 60px;
    padding: 0 20px;
  }

  .profile-info {
<<<<<<< HEAD
    width: 62.75%;
  }

  .section-head {
    margin-bottom: 10px;
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
  }

  .profile-name {
    color: #000;
    font-family: "Zen Old Mincho";
    font-size: 64px;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    letter-spacing: 9.6px;
=======
    max-width: 60%;
  }

  .profile-name {
    font-size: 48px;
    margin-bottom: 20px;
    letter-spacing: 0.1em;
    font-weight: 500;
>>>>>>> 246114c9b5c422ea4b070944e480aabcd002ad44
  }

  .social-links {
    display: flex;
    gap: 15px;
<<<<<<< HEAD
    align-items: center;
  }
  .social-icon {
    width: 50px;
    height: 50px;
    display: inline-block;
    padding: 10px 10px;
  }

  .social-icon img {
    cursor: pointer;
    filter: invert(59%) sepia(1%) saturate(380%) hue-rotate(24deg) brightness(103%) contrast(99%);
  }


  .social-icon:hover {
    background-color: #e0dede;
  }

  .social-icon:hover img {
    filter: invert(100%) sepia(100%) saturate(0%) hue-rotate(335deg) brightness(106%) contrast(102%);
  }

  .profile-meta {
    display: flex;
    flex-direction: column;
    gap: 5px;
    flex-wrap: wrap;
    margin-bottom: 30px;
  }

  .tags {
    color: #909090;
    font-family: "Zen Kurenaido";
    font-size: 16px;
    font-style: normal;
    font-weight: 200;
    line-height: normal;
    letter-spacing: 3px;
  }

  .profile-meta > div {
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    gap: 20px;
  }

  .profile-bio {
    white-space: pre-wrap;
    display: flex;
    align-items: center;
  }

  .profile-bio p {
    color: #000;
    font-family: "Zen Kurenaido";
    font-size: 18px;
    font-style: normal;
    font-weight: 400;
    line-height: 1.5;
    letter-spacing: 3px;
  }

  .grade-circle-wrapper {
    width: 300px;
    height: 300px;
    flex-shrink: 0;
    margin-top: 20px;
    z-index: 1 !important;
    position: relative;
  }
  
  .grade-circle {
    width: 100%;
    height: 100%;
    border: 1px solid #ccc;
    border-radius: 50%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    z-index: 1 !important;
=======
    margin-bottom: 20px;
  }
  .social-icon {
      display: flex;
      align-items: center;
      gap: 5px;
      font-size: 0.9rem;
      color: #888;
      text-decoration: none;
  }

  .profile-meta {
    font-family: "Courier New", monospace;
    font-size: 14px;
    color: #666;
    margin-bottom: 30px;
    line-height: 1.8;
  }

  .profile-bio {
    font-size: 16px;
    line-height: 2;
    margin-bottom: 30px;
    white-space: pre-wrap;
  }

  .contact-text {
    font-size: 14px;
    color: #888;
  }

  .grade-circle-wrapper {
      position: relative;
      width: 200px;
      height: 200px;
      flex-shrink: 0; 
  }
  
  .grade-circle {
      width: 100%;
      height: 100%;
      background-color: #ddd;
      border-radius: 50%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      position: relative;
      overflow: hidden;
>>>>>>> 246114c9b5c422ea4b070944e480aabcd002ad44
  }

  .grade-icon-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

<<<<<<< HEAD
  .grade-text-overlay {
    position: absolute;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 2 !important;
    left: -102px;
    top: 37px;
  }

  .grade-num {
    color: #555;
    font-family: "Grandiflora One";
    font-size: 200px;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    letter-spacing: 30px;
  }
  .grade-txt {
    color: #000;
    font-family: "Grandiflora One";
    font-size: 64px;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    letter-spacing: 9.6px;
=======
  .grade-num {
      font-family: "Didot", serif;
      font-size: 100px;
      line-height: 1;
      font-weight: 400;
      position: absolute;
      left: -20px;
      top: 30px;
  }
  .grade-txt {
      font-family: "Courier New", monospace;
      font-size: 32px;
      position: absolute;
      right: 20px;
      bottom: 60px;
      color: #333;
>>>>>>> 246114c9b5c422ea4b070944e480aabcd002ad44
  }

  .divider-line {
      width: 100%;
      height: 1px;
      background: #ccc;
      margin-bottom: 40px;
  }

  .photos-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 20px;
      font-family: "Courier New", monospace;
      color: #888;
  }
  
  .click-hint {
<<<<<<< HEAD
    color: #909090;
    font-family: "Zen Kurenaido";
    font-size: 18px;
    font-style: normal;
    font-weight: 400;
    line-height: normal;
    letter-spacing: 3px;
    display: inline-block;
=======
      font-family: cursive;
      transform: rotate(-10deg);
      display: inline-block;
>>>>>>> 246114c9b5c422ea4b070944e480aabcd002ad44
  }

  .photo-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 15px;
  }

  .photo-item {
<<<<<<< HEAD
    aspect-ratio: 1 / 1;
    background: #eee;
    overflow: hidden;
  }
  
  .photo-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s ease;
  }
  
  .photo-item:hover img {
    transform: scale(1.05);
  }


  @media (max-width: 768px) {
    .section-title h1 {
      font-size: 50px;
    }
    .profile-section {
      flex-direction: column-reverse;
      gap: 40px;
      align-items: center;
    }
    .profile-info {
      max-width: 100%;
    }
    .grade-circle-wrapper {
      align-self: center;
      margin-bottom: 20px;
      width: 150px;
      height: 150px;
    }
    .grade-num { font-size: 80px; left: -10px; top: 20px; }
    .grade-txt { font-size: 24px; right: 10px; bottom: 40px; }
=======
      aspect-ratio: 1 / 1;
      background: #eee;
      overflow: hidden;
  }
  
  .photo-item img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.3s ease;
  }
  
  .photo-item:hover img {
      transform: scale(1.05);
  }

  @media (max-width: 768px) {
      .section-title h1 {
          font-size: 50px;
      }
      .profile-section {
          flex-direction: column-reverse;
          gap: 40px;
          align-items: center;
      }
      .profile-info {
          max-width: 100%;
      }
      .grade-circle-wrapper {
          align-self: center;
          margin-bottom: 20px;
          width: 150px;
          height: 150px;
      }
      .grade-num { font-size: 80px; left: -10px; top: 20px; }
      .grade-txt { font-size: 24px; right: 10px; bottom: 40px; }
>>>>>>> 246114c9b5c422ea4b070944e480aabcd002ad44
  }
</style>